<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="Career Guide"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar"
        android:supportsRtl="true">

        <!-- Start with a simple LoginActivity for syllabus demonstration -->
        <activity android:name=".LoginActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".QuizActivity" android:exported="false" />
        <activity android:name=".ResultActivity" android:exported="false" />
        <activity android:name=".CareerListActivity" android:exported="false" />
        <activity android:name=".CareerDetailsActivity" android:exported="false" />
        <activity android:name=".SavedCareersActivity" android:exported="false" />
        <activity android:name=".SettingsActivity" android:exported="false" />
        <activity android:name=".AboutActivity" android:exported="false" />
        <activity android:name=".WebViewActivity" android:exported="false" />

        <!-- ContentProvider for saved careers -->
        <provider
            android:name=".CareerProvider"
            android:authorities="com.example.careerguide.provider"
            android:exported="false" />

    </application>

</manifest>
